# bridge-exec-bot

Historical only. This package is retained as a reference artifact from the original bridge-assisted execution design and is not part of the active Base/Arbitrum runtime.

The current deployment path is the direct Base/Arbitrum topology documented in `docs/MIGRATION-BASE-ARB.md` and `STATUS.md`.