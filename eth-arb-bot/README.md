# eth-arb-bot

Historical only. This package is retained as a reference artifact from the original Ethereum execution path and is not part of the active Base/Arbitrum runtime.

Use `arb-bot` for the current execution path.

For migration status and current runtime details, see `docs/MIGRATION-BASE-ARB.md` and `STATUS.md`.